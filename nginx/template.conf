# Configuración Nginx para Laravel con Cloudflare SSL - Generado automáticamente
# Proyecto: PROJECT_NAME
# Dominio: DOMAIN_NAME
# PHP Version: PHP_VERSION
# SSL: Cloudflare Full (Strict)

# Redirección HTTP a HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name DOMAIN_NAME www.DOMAIN_NAME;

    # Redireccionar todo el tráfico HTTP a HTTPS
    return 301 https://$server_name$request_uri;
}

# Configuración principal HTTPS con Cloudflare
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name DOMAIN_NAME www.DOMAIN_NAME;

    # Directorio raíz del proyecto Laravel
    root /var/www/PROJECT_NAME/public;
    index index.php index.html index.htm;

    # Configuración SSL con Cloudflare Origin Certificate
    ssl_certificate /etc/ssl/certs/cloudflare-origin.pem;
    ssl_certificate_key /etc/ssl/private/cloudflare-origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_dhparam /etc/ssl/certs/dhparam.pem;

    # Headers de seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=(), usb=(), magnetometer=(), gyroscope=(), speaker=(), vibrate=(), fullscreen=(self), sync-xhr=()" always;

    # Content Security Policy - Ajustar según necesidades
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https:; media-src 'self'; object-src 'none'; child-src 'none'; worker-src 'none'; frame-ancestors 'none'; form-action 'self'; base-uri 'self';" always;

    # Configuración de IPs reales desde Cloudflare
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 131.0.72.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 2400:cb00::/32;
    set_real_ip_from 2606:4700::/32;
    set_real_ip_from 2803:f800::/32;
    set_real_ip_from 2405:b500::/32;
    set_real_ip_from 2405:8100::/32;
    set_real_ip_from 2c0f:f248::/32;
    set_real_ip_from 2a06:98c0::/29;
    real_ip_header CF-Connecting-IP;

    # Logs
    access_log /var/log/nginx/PROJECT_NAME.access.log;
    error_log /var/log/nginx/PROJECT_NAME.error.log;

    # Configuración de compresión Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types
        application/atom+xml
        application/javascript
        application/json
        application/ld+json
        application/manifest+json
        application/rss+xml
        application/vnd.geo+json
        application/vnd.ms-fontobject
        application/x-font-ttf
        application/x-web-app-manifest+json
        application/xhtml+xml
        application/xml
        font/opentype
        image/bmp
        image/svg+xml
        image/x-icon
        text/cache-manifest
        text/css
        text/plain
        text/vcard
        text/vnd.rim.location.xloc
        text/vtt
        text/x-component
        text/x-cross-domain-policy;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=PROJECT_NAME_login:10m rate=1r/s;
    limit_req_zone $binary_remote_addr zone=PROJECT_NAME_api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=PROJECT_NAME_general:10m rate=2r/s;

    # Aplicar rate limiting a endpoints de login
    location ~ ^/(login|register|password/reset) {
        limit_req zone=PROJECT_NAME_login burst=5 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Aplicar rate limiting a endpoints de API
    location ~ ^/api/ {
        limit_req zone=PROJECT_NAME_api burst=20 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Manejo principal de rutas Laravel
    location / {
        limit_req zone=PROJECT_NAME_general burst=10 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Procesamiento de archivos PHP
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/phpPHP_VERSION-fpm-PROJECT_NAME.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;

        # Parámetros de seguridad
        fastcgi_param HTTP_PROXY "";
        fastcgi_param HTTPS on;
        fastcgi_param SERVER_PORT 443;
        fastcgi_param REQUEST_SCHEME https;

        # Timeouts optimizados
        fastcgi_connect_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_read_timeout 60s;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    # Denegar acceso a archivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Denegar acceso a archivos sensibles de Laravel
    location ~ ^/(\.env|\.git|composer\.(json|lock)|package\.json|yarn\.lock|webpack\.mix\.js|artisan|phpunit\.xml|\.htaccess|\.htpasswd) {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Cache para archivos estáticos
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|txt|tar|gz|woff|woff2|ttf|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Pragma public;
        add_header Vary Accept-Encoding;
        access_log off;
    }

    # Manejo especial para favicon
    location = /favicon.ico {
        access_log off;
        log_not_found off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Manejo especial para robots.txt
    location = /robots.txt {
        access_log off;
        log_not_found off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Prevenir acceso a directorios de storage y cache
    location ~ ^/(storage|bootstrap/cache) {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Bloquear patrones de ataque comunes
    location ~* (base64_encode|base64_decode|benchmark|union|select|insert|cast|set|declare|drop|update|md5|concat|script|javascript|vbscript|onload|onerror) {
        deny all;
        access_log off;
        log_not_found off;
        return 444;
    }

    # Bloquear intentos de inyección SQL
    location ~* (union.*select|insert.*into|delete.*from|drop.*table) {
        deny all;
        access_log off;
        log_not_found off;
        return 444;
    }

    # Prevenir acceso a archivos de respaldo y temporales
    location ~* ^.+\.(bak|config|sql|fla|psd|ini|log|sh|inc|swp|dist)|~$ {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Configuración especial para Laravel Horizon (si está instalado)
    location ~ ^/horizon {
        try_files $uri $uri/ /index.php?$query_string;

        # Restricción de acceso opcional (descomentar si necesario)
        # allow 127.0.0.1;
        # allow YOUR_OFFICE_IP;
        # deny all;
    }

    # Configuración para WebSockets (Laravel Broadcasting)
    location /socket.io/ {
        proxy_pass http://127.0.0.1:6001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Ocultar información del servidor
    server_tokens off;
    more_set_headers "Server: Laravel/PROJECT_NAME";

    # Configuración de buffer para mejorar rendimiento
    client_body_buffer_size 128k;
    client_max_body_size 64M;
    client_header_buffer_size 1k;
    large_client_header_buffers 4 4k;
    output_buffers 1 32k;
    postpone_output 1460;

    # Configuración de keepalive
    keepalive_timeout 65;
    keepalive_requests 100;

    # Configuración de sendfile
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
}
    }

    # Manejo principal de rutas Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Procesamiento de archivos PHP
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/phpPHP_VERSION-fpm-PROJECT_NAME.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;

        # Parámetros de seguridad
        fastcgi_param HTTP_PROXY "";
        fastcgi_param HTTPS on;
        fastcgi_param SERVER_PORT 443;
        fastcgi_param REQUEST_SCHEME https;

        # Timeouts optimizados
        fastcgi_connect_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_read_timeout 60s;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    # Denegar acceso a archivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Denegar acceso a archivos sensibles de Laravel
    location ~ ^/(\.env|\.git|composer\.(json|lock)|package\.json|yarn\.lock|webpack\.mix\.js|artisan|phpunit\.xml|\.htaccess|\.htpasswd) {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Cache para archivos estáticos
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|txt|tar|gz|woff|woff2|ttf|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Pragma public;
        add_header Vary Accept-Encoding;
        access_log off;
    }

    # Manejo especial para favicon
    location = /favicon.ico {
        access_log off;
        log_not_found off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Manejo especial para robots.txt
    location = /robots.txt {
        access_log off;
        log_not_found off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Prevenir acceso a directorios de storage y cache
    location ~ ^/(storage|bootstrap/cache) {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Bloquear patrones de ataque comunes
    location ~* (base64_encode|base64_decode|benchmark|union|select|insert|cast|set|declare|drop|update|md5|concat|script|javascript|vbscript|onload|onerror) {
        deny all;
        access_log off;
        log_not_found off;
        return 444;
    }

    # Bloquear intentos de inyección SQL
    location ~* (union.*select|insert.*into|delete.*from|drop.*table) {
        deny all;
        access_log off;
        log_not_found off;
        return 444;
    }

    # Prevenir acceso a archivos de respaldo y temporales
    location ~* ^.+\.(bak|config|sql|fla|psd|ini|log|sh|inc|swp|dist)|~$ {
        deny all;
        access_log off;
        log_not_found off;
        return 404;
    }

    # Configuración especial para Laravel Horizon (si está instalado)
    location ~ ^/horizon {
        try_files $uri $uri/ /index.php?$query_string;

        # Restricción de acceso opcional (descomentar si necesario)
        # allow 127.0.0.1;
        # allow YOUR_OFFICE_IP;
        # deny all;
    }

    # Configuración para WebSockets (Laravel Broadcasting)
    location /socket.io/ {
        proxy_pass http://127.0.0.1:6001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Ocultar información del servidor
    server_tokens off;
    more_set_headers "Server: Laravel/PROJECT_NAME";

    # Configuración de buffer para mejorar rendimiento
    client_body_buffer_size 128k;
    client_max_body_size 64M;
    client_header_buffer_size 1k;
    large_client_header_buffers 4 4k;
    output_buffers 1 32k;
    postpone_output 1460;

    # Configuración de keepalive
    keepalive_timeout 65;
    keepalive_requests 100;

    # Configuración de sendfile
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
}